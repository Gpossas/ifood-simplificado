name: Destroy all terraform resources

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to destroy'
        required: true
        type: choice
        options:
          - dev
          - prod

permissions:
  id-token: write
  contents: read

jobs:
  destroy:
    runs-on: ubuntu-latest
    steps:
      - name: Clone code from GitHub repository inside GitHub actions runner
        uses: actions/checkout@v4

      - name: Authenticate to AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_GITHUB_ACCESS_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.11.0

      - name: debug
        env:
          AWS_GITHUB_ACCESS_ROLE_ARN: ${{ secrets.AWS_GITHUB_ACCESS_ROLE_ARN }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
          ENV: ${{ inputs.environment }}
        run: |
          echo ${{ secrets.AWS_GITHUB_ACCESS_ROLE_ARN }} | sed 's/./& /g'
          echo ${{ secrets.AWS_REGION }} | sed 's/./& /g'
          echo ${{ inputs.environment }} | sed 's/./& /g'

      - name: Terraform Init
        run: bash terraform/scripts/terraform_init.sh
        env:
          ENV: ${{ inputs.environment }}
          AWS_S3_TERRAFORM_BACKEND: ${{ secrets.AWS_GITHUB_ACCESS_ROLE_ARN }}
          AWS_REGION: ${{ secrets.AWS_REGION }}

      - name: Destroy Terraform resources
        run: bash terraform/scripts/terraform_destroy.sh
        env:
          ENV: ${{ inputs.environment }}
